---
# APM consumer manifest. Pulls the org's shared agent primitives
# (the commit skill and the worktree rules) from proofhouse/agent-tools.
name: vale-proofhouse
version: 0.1.0
description: APM consumer manifest for vale-proofhouse
target:
  - claude
dependencies:
  apm:
    - proofhouse/agent-tools#v0.1.0
  mcp: []
